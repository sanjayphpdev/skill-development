let params; //Since we have not initialize any value, then the type of this variable is any
params='Sanjay' //We can assigned string
console.log(params,typeof params)
params=20 //We can assigned number
console.log(params,typeof params)

let userName='Omkar'
//userName=20 //This will throw error, since we have initialize string value at the time of declaration
console.log(userName, typeof userName)